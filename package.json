{
  "name": "yt-dl-studio",
  "version": "1.0.0",
  "description": "A modern GUI for yt-dlp — Go backend + React frontend",
  "scripts": {
    "predev:backend": "node scripts/kill-port.mjs 7391",
    "dev:backend": "node scripts/dev-backend.mjs",
    "dev:frontend": "npm run dev --prefix web",
    "dev": "concurrently --kill-others -n backend,frontend -c magenta,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix web",
    "build": "npm run build:frontend && go build -o yt-dls.exe ./cmd/server",
    "start": "go run ./cmd/server",
    "test": "go test ./..."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3"
  }
}
